# Plane.so
PLANE_API_TOKEN=your-plane-api-token-here

# GitHub
GITHUB_TOKEN=your-github-token-here

# AI Providers (OAuth preferred)
ANTHROPIC_API_KEY=your-anthropic-key
OPENAI_API_KEY=your-openai-key

# Optional overrides
ORCHESTRATOR_PORT=4000

# ---------------------------------------------------------------------------
# Production (Coolify deployment)
# ---------------------------------------------------------------------------
DOMAIN=zenova.id
PLANE_DOMAIN=plane.zenova.id
PLANE_SPACE_DOMAIN=space.zenova.id
ORCHESTRATOR_DOMAIN=orchestrator.zenova.id
SECRET_KEY=<generate-a-secret>

# Database
POSTGRES_USER=plane
POSTGRES_PASSWORD=<generate-a-strong-password>
POSTGRES_DB=plane

# MinIO / S3
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=<generate-a-strong-password>
AWS_S3_BUCKET_NAME=plane-uploads
AWS_REGION=us-east-1

# Plane settings
ENABLE_SIGNUP=0
GUNICORN_WORKERS=4
FILE_SIZE_LIMIT=5242880
